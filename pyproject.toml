[project]
name = "open-source-tracking"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "marimo>=0.10.12",
    "rootutils>=1.0.7",
    "ultralytics>=8.3.61",
]
